name: mSearch

on:
  push:
    branches:
      - master

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      # 📦 Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # ⚡ Node moderne + cache Yarn
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      # 🔧 Force npm registry
      - name: Fix npm registry
        run: yarn config set registry https://registry.npmjs.org

      # 📦 Install dependencies
      - name: Install dependencies
        run: yarn install --frozen-lockfile
        env:
          ADBLOCK: true

      # 🐍 Installer Python (universel)
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # 🔐 Build with OpenSSL legacy provider
      - name: Build the app
        run: NODE_OPTIONS=--openssl-legacy-provider yarn ci-build
        env:
          GH_TOKEN: ${{ secrets.token }}
          MAC_CERTS: ${{ secrets.mac_certs }}
          MAC_CERTS_PASSWORD: ${{ secrets.mac_certs_password }}
          RELEASE: false

      # ⚠️ Désactiver la signature macOS si pas de certificat
      - name: Skip macOS signing if no certificate
        if: runner.os == 'macOS'
        run: |
          echo "sign=false" >> electron-builder.json
